#include<iostream>
using namespace std;
int byteland(int m[],int n)
{
    if(n<1000000)
        if(m[n]!=-1)return m[n];
    if(n>n/2+n/3)
}
int main()
{
    int n;
    int m[1000000];
    for(int i=0;i<=1000000;i++)
    {
        m[i]=-1;
    }
    cout<<byteland(n);
    return 0;
}
